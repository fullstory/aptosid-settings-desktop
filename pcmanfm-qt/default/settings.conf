# aptosid default profile, shared via aptosid-settings-common.
[System]
# Only applied when the desktop gives Qt no icon theme (e.g. Hyprland);
# a full DE like LXQt keeps its own, so one file suits both.
FallbackIconThemeName=Mint-Y-Blue
Terminal=x-terminal-emulator
